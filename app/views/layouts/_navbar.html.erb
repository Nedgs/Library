<nav class="navbar navbar-expand-lg navbar-light bg-custom">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= books_path %>">
      <img src="<%= asset_path('logo.png') %>" alt="Logo" style="height: 50px;">
    </a>
    <a class="navbar-brand" href="<%= books_path %>">Books</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if logged_in? %>
          <li class="nav-item">
            <a class="nav-link" href="#">Welcome, <%= current_user.email %></a>
          </li>
          <li class="nav-item">
            <%= link_to 'Logout', logout_path, method: :delete, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Login', login_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Register', register_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
