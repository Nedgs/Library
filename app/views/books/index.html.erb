<h1>Books</h1>

<%= link_to 'Add new book', new_book_path %>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book_path(book) %>
      by <%= book.author %>
    </li>
  <% end %>
</ul>
